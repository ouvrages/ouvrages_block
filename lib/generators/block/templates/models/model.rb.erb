class <%= standard_block_class_name %> < ApplicationRecord
  belongs_to :parent, polymorphic: true

  def self.permitted_attributes
    <%= [:id, :position, :_destroy].concat(attributes.map(&:name).map(&:to_sym)) %>
  end
end
